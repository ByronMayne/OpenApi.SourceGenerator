{{> header}}

using System.Threading;
using Microsoft.AspNetCore.Mvc;

namespace {{Globals.RootNamespace}}.Commands
{
    public partial class {{Model.MethodName}}Command : I{{Model.MethodName}}Command
    {
        public partial Task<IActionResult> ExecuteAsync({{#each Model.Parameters}}{{Schema}} {{Name}}, {{/each}}CancellationToken cancellationToken);
    }
}