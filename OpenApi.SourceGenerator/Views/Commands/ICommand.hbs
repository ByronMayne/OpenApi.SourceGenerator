{{> header}}

using System.Threading;
using Microsoft.AspNetCore.Mvc;

namespace {{Globals.RootNamespace}}.Commands
{
    public interface I{{Model.MethodName}}Command
    {
        Task<IActionResult> ExecuteAsync({{#each Model.Parameters}}{{Schema}} {{Name}}, {{/each}}CancellationToken cancellationToken);
    }
}